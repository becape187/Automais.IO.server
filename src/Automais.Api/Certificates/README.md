Coloque aqui o arquivo `ca-certificate.crt` fornecido pelo DigitalOcean.

O arquivo **não** é versionado (ver `.gitignore`). Ele será carregado pela API via configuração `Database:RootCertificatePath`. Certifique-se de usar o mesmo nome (`ca-certificate.crt`) ou atualize o caminho no `appsettings`.


